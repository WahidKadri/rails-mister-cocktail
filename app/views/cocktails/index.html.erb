<div class="banner">
  <div class="banner-content">
    <h1><i class="fas fa-glass-martini"></i> Mister Cocktails</h1>
    <ul class="list-unstyled">
        <% @cocktails.each do |cocktail| %>
          <li>
            <%= link_to cocktail.name, cocktail_path(cocktail) %>
          </li>
          <% end %>
      </ul>
  </div>
</div>
